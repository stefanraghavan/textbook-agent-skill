# CONVERGING TEES

A momentum balance in the $x$-direction gives:

$$
A_1(P_2 - P_1) = \frac{V_1\dot{w}_1}{g} - \frac{V_2\dot{w}_2}{g} - \frac{V_x\dot{w}_3}{g} - \frac{C_MV_1\dot{w}_3}{g},
$$

where the last term accounts for the uncertainty associated with fluid from the lateral channel, or branch, piercing the flow field that is a violation of the model. The coefficients $C_{xC}$ and $C_M$ are defined in Equations 16.23 and 16.24 below. We expect $V_x < V_2$ and express this as $V_x = C_{xC}V_2$. By using this relationship and rearranging, the momentum equation becomes:

$$
P_2 - P_1 = \frac{1}{gA_1}(V_1\dot{w}_1 - V_2\dot{w}_2 - C_{xC}V_2\dot{w}_3 - C_MV_1\dot{w}_3).
$$

By use of the continuity equations $\dot{w}_3 = \dot{w}_1 - \dot{w}_2$, $\dot{w}_1 = V_1A_1\rho_w$, and $\dot{w}_2 = V_2A_2\rho_w$, the momentum equation becomes:

$$
P_2 - P_1 = \frac{\rho_wV_1^2}{g}\left[1 - \frac{V_2^2}{V_1^2} - C_{xC}\left(\frac{V_2}{V_1} - \frac{V_2^2}{V_1^2}\right) - C_M\left(1 - \frac{V_2}{V_1}\right)\right].
$$

$$
(16.20)
$$

Substitution of Equation 16.20 into Equation 16.19, and letting $V_1 = \dot{w}_1/A_1\rho_w$ and $V_2 = \dot{w}_2/A_1\rho_w$ gives:

$$
K_{21}' = 1 - \frac{\dot{w}_2^2}{\dot{w}_1^2} - 2C_{xC}\left(\frac{\dot{w}_2}{\dot{w}_1} - \frac{\dot{w}_2^2}{\dot{w}_1^2}\right) - 2C_M\left(1 - \frac{\dot{w}_2}{\dot{w}_1}\right).
$$

$$
(16.21)
$$

Equation 16.21 produces a value of zero when there is no flow from the branch, that is, when $\dot{w}_2 = \dot{w}_1$. Actually a small loss results from the enlargement and expansion of flow across the branch opening. The second term in the equation was modified to account for this loss:

$$
K_{21} = 1 - 0.95\frac{\dot{w}_2^2}{\dot{w}_1^2} - 2C_{xC}\left(\frac{\dot{w}_2}{\dot{w}_1} - \frac{\dot{w}_2^2}{\dot{w}_1^2}\right) - 2C_M\left(1 - \frac{\dot{w}_2}{\dot{w}_1}\right).
$$

$$
(16.22)
$$

Correlation with data from various investigators indicates that the loss coefficient for flow through run in the converging flow case is virtually independent of the diameter ratio $d_3/d_1$, but is a strong function of the radius of curvature $r$ of the joining edge. The coefficients $C_{xC}$ and $C_M$ in Equation 16.22 are functions of $r$ and may be determined by the following equations for $r/d_3 \leq 0.3$:

$$
C_M = 0.23 + 1.46(r/d_3) - 2.75(r/d_3)^2 + 1.65(r/d_3)^3
$$

$$
(16.23)
$$

and

$$
C_{xC} = 0.08 + 0.56(r/d_3) - 1.75(r/d_3)^2 + 1.83(r/d_3)^3.
$$

$$
(16.24)
$$

Results from Equation 16.22 are compared with data from various sources in Figure 16.9. There is general

![FIGURE 16.9](figures/page_0189_fig_01.png)
<!-- Description: Graph comparing Equation 16.22 with results from other investigators for converging flow through run of tee; plot of $K_{21}$ versus $\dot{w}_2/\dot{w}_1$ with multiple curves for different $r/d_3$ values and a legend. -->

**FIGURE 16.9.** Comparison of Equation 16.22 with results from other investigators for the case of converging flow through run of tee.