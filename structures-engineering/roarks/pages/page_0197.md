# TABLE 8.1 Shear, moment, slope, and deflection formulas for elastic straight beams *(Continued)*

| End restraints, reference no. | Boundary values | Selected maximum values of moments and deformations |
|---|---|---|
| 2b. Left end guided, right end fixed

![Figure 8.1: 2b. Left end guided, right end fixed](figures/fig8_1_2b.png)
<!-- Description: Beam with left end guided and right end fixed, carrying a linearly varying distributed load from $w_a$ to $w_l$ over length $(l-a)$ starting at distance $a$ from the left end. --> | $R_A=0 \qquad \theta_A=0$

$$
M_A=\frac{w_a}{6l}(l-a)^3+\frac{w_l-w_a}{24l}(l-a)^3
$$

$$
y_A=-\frac{w_a}{24EI}(l-a)^3(l+a)-\frac{w_l-w_a}{240EI}(l-a)^3(3l+2a)
$$

$$
R_B=\frac{w_a+w_l}{2l}(l-a)
$$

$$
M_B=-\frac{w_a}{6l}(l-a)^2(2l+a)-\frac{w_l-w_a}{24l}(l-a)^2(3l+a)
$$

$$
\theta_B=0 \qquad y_B=0
$$ | If $a=0$ and $w_l=w_a$ (uniform load on entire span), then

$$
\text{Max }-M=M_B=-\frac{w_al^2}{3}
\qquad
\text{Max }+M=M_A=\frac{w_al^2}{6}
$$

$$
\text{Max }y=y_A=-\frac{w_al^4}{24EI}
$$

If $a=0$ and $w_a=0$ (uniformly increasing load), then

$$
\text{Max }-M=M_B=-\frac{w_ll^2}{8}
\qquad
\text{Max }+M=M_A=\frac{w_ll^2}{24}
$$

$$
\text{Max }y=y_A=-\frac{w_ll^4}{80EI}
$$

If $a=0$ and $w_l=0$ (uniformly decreasing load), then

$$
\text{Max }-M=M_B=-\frac{5w_al^2}{24}
\qquad
\text{Max }+M=M_A=\frac{w_al^2}{8}
$$

$$
\text{Max }y=y_A=-\frac{7w_al^4}{240EI}
$$ |
| 2c. Left end simply supported, right end fixed

![Figure 8.1: 2c. Left end simply supported, right end fixed](figures/fig8_1_2c.png)
<!-- Description: Beam with left end simply supported and right end fixed, carrying a linearly varying distributed load from $w_a$ to $w_l$ over length $(l-a)$ starting at distance $a$ from the left end. --> | $R_A=\frac{w_a}{8l^3}(l-a)^3(3l+a)+\frac{w_l-w_a}{40l^3}(l-a)^3(4l+a)$

$$
\theta_A=-\frac{w_a}{48EI\,l}(l-a)^3(l+3a)-\frac{w_l-w_a}{240EI\,l}(l-a)^3(2l+3a)
$$

$$
M_A=0 \qquad y_A=0
$$

$$
R_B=\frac{w_a+w_l}{2}(l-a)-R_A
$$

$$
M_B=R_Al-\frac{w_a}{2}(l-a)^2-\frac{w_l-w_a}{6}(l-a)^2
$$

$$
\theta_B=0 \qquad y_B=0
$$ | If $a=0$ and $w_l=w_a$ (uniform load on entire span), then

$$
R_A=\frac{3}{8}w_al \qquad R_B=\frac{5}{8}w_al \qquad \text{Max }-M=M_B=-\frac{w_al^2}{8}
$$

$$
\text{Max }+M=\frac{9w_al^2}{128}\text{ at }x=\frac{3}{8}l
\qquad
\text{Max }\theta=\theta_A=-\frac{w_al^3}{48EI}
$$

$$
\text{Max }y=-0.0054\frac{w_al^4}{EI}\text{ at }x=0.4215l
$$

If $a=0$ and $w_a=0$ (uniformly increasing load), then

$$
R_A=\frac{wl}{10} \qquad R_B=\frac{2wl}{5} \qquad \text{Max }-M=M_B=-\frac{wl^2}{15}
$$

$$
\text{Max }+M=0.0298wl^2\text{ at }x=0.4472l
\qquad
\text{Max }\theta=\theta_A=-\frac{wl^3}{120EI}
$$

$$
\text{Max }y=-0.00239\frac{wl^4}{EI}\text{ at }x=0.4472l
$$

If $a=0$ and $w_l=0$ (uniformly decreasing load), then

$$
R_A=\frac{11}{40}w_al \qquad R_B=\frac{9}{40}w_al \qquad \text{Max }-M=M_B=-\frac{7}{120}w_al^2
$$

$$
\text{Max }+M=0.0422w_al^2\text{ at }x=0.329l
$$

$$
\text{Max }\theta=\theta_A=-\frac{w_al^3}{80EI}
\qquad
\text{Max }y=-0.00304\frac{w_al^4}{EI};\text{ at }x=0.4025l
$$ |

192

Formulas for Stress and Strain

[CHAP. 8]