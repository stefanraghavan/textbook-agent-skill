SEC. 2.3]

# Stress and Strain: Important Relationships

![Figure 2.9: Coordinate transformation.](figures/fig2_9.png)
<!-- Description: A coordinate transformation diagram showing original axes $x$, $y$, $z$ and rotated axes $x'$, $y'$, $z'$, with angles $\theta_{x'x}$, $\theta_{x'y}$, and $\theta_{x'z}$ indicated between the $x'$ axis and the original coordinate axes. -->

**Figure 2.9**  Coordinate transformation.

transforms a vector given in $xyz$ coordinates, $\{\mathbf{V}\}_{xyz}$, to a vector in $x'y'z'$ coordinates, $\{\mathbf{V}\}_{x'y'z'}$, by the matrix multiplication

$$
\{\mathbf{V}\}_{x'y'z'} = [\mathbf{T}]\{\mathbf{V}\}_{xyz}
\tag{2.3-7}
$$

Furthermore, it can be shown that the transformation equation for the stress matrix is given by (see Ref. 1)

$$
[\boldsymbol{\sigma}]_{x'y'z'} = [\mathbf{T}][\boldsymbol{\sigma}]_{xyz}[\mathbf{T}]^T
\tag{2.3-8}
$$

where $[\mathbf{T}]^T$ is the *transpose* of the transformation matrix $[\mathbf{T}]$, which is simply an interchange of rows and columns. That is,

$$
[\mathbf{T}]^T =
\begin{bmatrix}
l_{x'} & l_{y'} & l_{z'} \\
m_{x'} & m_{y'} & m_{z'} \\
n_{x'} & n_{y'} & n_{z'}
\end{bmatrix}
\tag{2.3-9}
$$

The stress transformation by Eq. (2.3-8) can be implemented very easily using a computer spreadsheet or mathematical software. Defining the directional cosines is another matter. One method is to define the $x'y'z'$ coordinate system by a series of two-dimensional rotations from the initial $xyz$ coordinate system. Table 2.2 at the end of this chapter gives transformation matrices for this.