# TABLE 8.8 Shear, moment, slope, and deflection formulas for beams under simultaneous axial compression and transverse loading *(Continued)*

| End restraints, reference no. | Boundary values | Selected maximum values of moments and deformations |
|---|---|---|
| 2b. Left end guided, right end fixed

![Figure 2b](figures/fig2b.png)
<!-- Description: Beam with left end guided and right end fixed, axial compressive load P at left, distance a to start of linearly varying distributed load increasing to w_l at the right end. --> | $R_A = 0 \qquad \theta_A = 0$

$$
M_A=\frac{w_a}{k^2}\frac{C_{a4}}{C_2}+\frac{w_l-w_a}{k^3(l-a)}\frac{C_{a5}}{C_2}
$$

$$
y_A=\frac{-w_a}{kP}\frac{C_3C_{a4}-C_2C_{a5}}{C_2}-\frac{w_l-w_a}{k^3P(l-a)}\frac{C_3C_{a5}-C_2C_{a6}}{C_2}
$$

$$
R_B=\frac{w_a+w_l}{2}(l-a)
\qquad
\theta_B=0
$$

$$
M_B=\frac{-w_a}{k^2}\frac{C_2C_{a3}-C_1C_{a4}}{C_2}
-\frac{(w_1-w_a)}{k^3(l-a)}\frac{C_2C_{a4}-C_1C_{a5}}{C_2}
$$

$$
y_B=0
$$ | If $a=0$ and $w_a=w_l$ (uniform load on entire span), then

$$
\text{Max }+M=M_A=\frac{w_a}{k^2}\left(\frac{kl}{\sin kl}-1\right)
$$

$$
\text{Max }-M=M_B=\frac{-w_a}{k^2}\left(1-\frac{kl}{\tan kl}\right)
$$

$$
\text{Max }y=y_A=\frac{-w_a l}{kP}\left(\tan\frac{kl}{2}-\frac{kl}{2}\right)
$$

If $a=0$ and $w_a=0$ (uniformly increasing load), then

$$
\text{Max }+M=M_A=\frac{w_l}{k^2}\left(\frac{kl}{2\sin kl}-\frac{\tan(kl/2)}{kl}\right)
$$

$$
\text{Max }-M=M_B=\frac{-w_l}{k^2}\left(1-\frac{kl}{2\tan kl}-\frac{1-\cos kl}{kl\sin kl}\right)
$$

$$
\text{Max }y=y_A=\frac{-w_l}{k^2P}\left[\left(\frac{kl}{2}-\frac{2}{kl}\right)\tan\frac{kl}{2}-\frac{k^2l^2}{6}+1\right]
$$ |
| 2c. Left end simply supported, right end fixed

![Figure 2c](figures/fig2c.png)
<!-- Description: Beam with left end simply supported and right end fixed, axial compressive load P at left, distance a to start of linearly varying distributed load increasing to w_l at the right end. --> | $M_A=0 \qquad y_A=0$

$$
R_A=\frac{w_a}{k}\frac{C_2C_{a4}-C_1C_{a5}}{C_2C_3-C_1C_4}
+\frac{w_l-w_a}{k^2(l-a)}\frac{C_2C_{a5}-C_1C_{a6}}{C_2C_3-C_1C_4}
$$

$$
\theta_A=\frac{-w_a}{kP}\frac{C_4C_{a4}-C_3C_{a5}}{C_2C_3-C_1C_4}
-\frac{w_l-w_a}{k^2P(l-a)}\frac{C_4C_{a5}-C_3C_{a6}}{C_2C_3-C_1C_4}
$$

$$
R_B=\frac{w_a+w_l}{2}(l-a)-R_A
$$

$$
M_B=\frac{-w_a}{k^2}\left(\frac{C_2C_{a5}-klC_2C_{a4}}{C_2C_3-C_1C_4}+C_{a3}\right)
$$

$$
-\frac{(w_l-w_a)}{k^3(l-a)}\left(\frac{C_2C_{a6}-klC_2C_{a5}}{C_2C_3-C_1C_4}+C_{a4}\right)
$$

$$
\theta_B=0 \qquad y_B=0
$$ | If $a=0$ and $w_a=w_l$ (uniform load on entire span), then

$$
\text{Max }\theta=\theta_A=\frac{-w_a}{kP}\frac{4-2kl\sin kl-(2-k^2l^2/2)(1+\cos kl)}{\sin kl-kl\cos kl}
$$

$$
\text{Max }-M=M_B=\frac{-w_a l\tan kl[\tan(kl/2)-kl/2]}{k(\tan kl-kl)}
$$

$$
R_A=\frac{w_a kl\sin kl-1+(1-k^2l^2/2)\cos kl}{k(\sin kl-kl\cos kl)}
$$

If $a=0$ and $w_a=0$ (uniformly increasing load), then

$$
\text{Max }\theta=\theta_A=\frac{-w_l}{6P}\frac{2kl+kl\cos kl-3\sin kl}{\sin kl-kl\cos kl}
$$

$$
\text{Max }-M=M_B=\frac{-w_l(l-k^2l^3/3)\tan kl-kl}{k^2(\tan kl-kl)}
$$

$$
R_A=\frac{w_l l}{6}\left(\frac{2\tan kl}{\tan kl-kl}-\frac{6}{k^2l^2}+1\right)
$$ |

232

Formulas for Stress and Strain

[CHAP. 8]